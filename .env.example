# Punjab Real Estate ML System — Environment Configuration
# DO NOT commit the actual .env file to version control.

# --- API Security ---
# Comma-separated list of valid API keys for authentication (x-api-key header)
API_KEYS=testkey_12345,devkey_67890,prodkey_abcde

# --- Database ---
# Path to SQLite databases (internal container paths)
# INFERENCE_DB_PATH=/app/data/logs/inference_logs.db

# --- MLflow ---
# MLFLOW_TRACKING_URI=sqlite:///mlruns.db
